# GAPS HUMANOID_BASE_v001

Extract into the repository root, then run:

```cmd
python Compiler\validate_humanoid_base.py
```

Expected:

```text
HUMANOID BASE VALIDATION: PASS
Required joints: 21
Rig parts: 15
```
